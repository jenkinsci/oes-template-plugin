
git:
  branch:
    meta:
      type: string
      default: master
  auth:
    meta:
      type: credential
      default: jenkins-gitlab
  url:
    meta:
      type: string

build:
  docker_auth:
    meta:
      type: credential
      default: opsbox-acr-auth
  docker_reg:
    meta:
      type: string
      default: registry.cn-chengdu.aliyuncs.com
  target_path:
    meta:
      type: string

